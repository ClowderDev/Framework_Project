@{
    ViewData["title"] = "Danh sách so sánh";
    var key = 0;
}

<!DOCTYPE html>

<html>

<head>
    <meta name="viewport" content="width=device-width" />

</head>

<body>
    <h3>Danh sách so sánh</h3>

    <div class="compare-container">
<<<<<<< HEAD
        @{
            var key = 0;
            bool isEmpty = true;
        }

        @foreach (var compare_product in Model)
        {
            isEmpty = false;
=======
        @foreach (var compare_product in Model)
        {
>>>>>>> 2ef18365088934809e564403ae838724d292a07b
            <div class="compare-item">
                <div class="product-image text-center">
                    <a asp-controller="Product" asp-action="Details" asp-route-Id="@compare_product.Product.Id">
                        <img src="~/media/products/@compare_product.Product.Image" width="150px" height="180px"
                            alt="@compare_product.Product.Name" />
                    </a>
                </div>
                <div class="product-details">
                    <a asp-controller="Product" asp-action="Details" asp-route-Id="@compare_product.Product.Id">
                        <h4 style="color: #E30019;">@compare_product.Product.Name</h4>
                    </a>
                    <p><strong>Giá:</strong>
                        <span style="color: rgb(254, 152, 15);">@compare_product.Product.Price.ToString("#,##0 VNĐ")</span>
                    </p>
                    <p><strong>Mô tả:</strong> @Html.Raw(@compare_product.Product.Description)</p>
                </div>
                <div class="product-action text-center">
                    <a class="btn btn-danger btn-sm confirmDeletion" asp-action="DeleteCompare" asp-controller="Home"
                        asp-route-Id="@compare_product.Compares.Id">Xóa</a>
                </div>
            </div>
        }
<<<<<<< HEAD
        @if (isEmpty)
        {
            <tr>
                <td colspan="6" style="padding: 20px; text-align: center; color: #888;">
                    <h3>Chưa có sản phẩm trong mục so sánh.</h3>
                </td>
            </tr>
        }
=======
>>>>>>> 2ef18365088934809e564403ae838724d292a07b
    </div>

    <style>
        .compare-container {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .compare-item {
            border: 1px solid #ddd;
            padding: 15px;
            margin: 10px;
            width: 30%;
            /* Adjust width based on desired number of items per row */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-shadow: 0 1px 2px 0 rgba(60, 64, 67, .1), 0 2px 6px 2px rgba(60, 64, 67, .15);
        }

        .product-image img {
            max-width: 100%;
            height: auto;
        }

        .product-details {
            margin-top: 15px;
            text-align: center;
        }

        .product-action {
            margin-top: 15px;
        }
    </style>
</body>

</html>